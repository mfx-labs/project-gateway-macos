# Project Gateway for macOS v0.2.0

## Highlights

- New one-time `pgw tunnel` setup for the macOS tunnel workflow.
- New normal-use `pgw up` command (foreground).
- `pgw help` command surface.
- Canonical `pgw project add / list / remove` commands.
- User-first README and onboarding workflow.

## Correctness

- **WP7-ERR-1 (`distinguish git config read failures from drift`):** an
  unreadable `.git/config` is now represented as a configuration-read failure
  and fails closed under the existing unsupported-state behavior, instead of
  being misreported as `.git/config content changed`.
- **Operator-controlled persistence v2:** the operator runtime now generates
  trusted workspace configuration `configurationVersion 2` with a
  deterministic workspace-local artifact location (`<workspace>/artifacts`),
  so controlled proposal persistence (`persist-artifact`) succeeds instead of
  being rejected by the version-2 destination-containment gate. Fresh
  registrations provision the artifact directory and bootstrap a version-2
  store. Proposal persistence remains proposal-only, create-only,
  destination-derived, containment-bound, and independently revalidated.
- **Legacy state policy:** pre-release `configurationVersion 1` state is
  unsupported and is not migrated; the supported path is clean
  uninstall/reset and fresh re-provisioning. No migration subsystem exists.

## Security

- `tunnel-client` pinned to v0.0.10.
- Official release origin pinned.
- Architecture-specific SHA-256 verification before installation.
- Runtime API Key persisted in the macOS Keychain.
- Literal secret not stored in the repository, tunnel profile, or logs.
- Foreground-only operation.
- No LaunchAgent / autostart installed by this workflow.
- Production trust-path and test-seam corrections completed.

## Documentation

- README rewritten as a user-first onboarding guide (Quick Start → install →
  tunnel setup → project registration → daily use → troubleshooting).

## Validation status

This section records the actual validation performed for v0.2.0.

### macOS Intel x86_64

Completed v0.2.0 acceptance on the native Intel lane:

- corrected x64 isolated installation acceptance passed;
- real x64 installation passed;
- `pgw tunnel` first-run setup passed;
- `pgw tunnel` idempotent reuse passed;
- the post-success `tmpdir` cleanup defect was found and corrected;
- the malformed stdio profile command `pgw start,channel=main` was found and
  corrected to `pgw start`, with `channel: main` carried as transport
  metadata;
- malformed owned `project-gateway` RC profiles are automatically reconciled;
- real `pgw up` foreground acceptance passed;
- OpenAI tunnel connectivity passed;
- ChatGPT read-only MCP `inspect-changes` end-to-end probe passed with
  `ok: true`;
- the live MCP inspection observed the expected nine changed RC paths;
- `Ctrl+C` foreground shutdown passed cleanly;
- installed-product MCP controlled persistence accepted end-to-end through
  the real runtime: `draft-artifact` (`valid: true`) → `persist-artifact`
  (`missing-to-file`) → proposal file created under the workspace `artifacts`
  directory → `inspect-changes` observed it; repeating the same request was
  denied by create-only containment (existing-target), the trusted store
  remained byte-identical, and no lifecycle/authority record was created;
- post-shutdown `pgw doctor` passed;
- no Project Gateway autostart / LaunchAgent / background daemon was
  introduced;
- native build-path privacy hygiene passed.

### macOS Apple Silicon arm64

- Static / package validation only.
- **No physical arm64 execution is claimed** for v0.2.0.

No claim is made that the complete historical/global `npm test` regression
suite is green for v0.2.0.
